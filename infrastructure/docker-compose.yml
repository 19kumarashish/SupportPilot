services:
  postgres:
    image: postgres:18
    container_name: supportpilot-postgres
    restart: unless-stopped
    env_file:
      - ../.env
    ports:
      - "5432:5432"
    volumes:
      - supportpilot_postgres_data:/var/lib/postgresql

volumes:
  supportpilot_postgres_data:
